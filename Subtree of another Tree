# the basic approach of me was when i find root == subroot, i use a helper function 


ill solve it again tomo



i did solve it but it is failing test cases my approach was use helper function to compare tree at every level, but it is failing or having any error 
this is the main program :

```python
        if not root:
            return False
        if not subRoot:
            return True
        
        
        if self.helper_sameTree(root,subRoot):
            return True
        self.isSubtree(root.left,subRoot) 
        self.isSubtree(root.right,subRoot)
        return False   

    def helper_sameTree(self,p,q):
        if not p and not q:
            return True
        if not p or not q or p.val != q.val:
            return False
        return self.helper_sameTree(p.left,q.left) and self.helper_sameTree(p.right,q.right)



#change the return line to this and the problem is solved :


return (self.isSubtree(root.left, subRoot)) or (self.isSubtree(root.right, subRoot)) --> #need to figure out why it works.


return (self.isSubtree(root.left, subRoot)) or (self.isSubtree(root.right, subRoot)) --> this line works 

        self.isSubtree(root.left,subRoot) 
        self.isSubtree(root.right,subRoot)  --> this does not 
        return False   


REASON --> Because in the downside i am calling the function but doing nothing with the result meanwhile in the working line both function are recursively called and 
even if result is found True means same tree is found at any side then or condition doesnt checks other side so it saves time.




