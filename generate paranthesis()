We call backtrack functions and can start appending but only when it follows certain conditions

we start by o,c set to 0 and we will increase them until o==c==n this will be the base case, earlier i thought to start from 0 and decrement till 0
both will work i will solve in both

conditions will be :
if o>0 u can append o or if o <n u can append 0 (in first case u have to stop when o ==0)
if c and c > o or if c < o (in second it means if opens are used and c is less so we can use c but never when c==o)

dfs :
in the if statement keep the first statement above as close paranthesi should never start over open and every call will be started from open
then after the first condition write dfs(o+1,c)
after it write stack.pop() because in the same recursive call it should pop and explore other options here we dont write it in end,

in second when all open options are explored then we write dfs(o,c+1) then write pop so all closed options should be covered

*doubt how is duplicates getting handled :
solved 
ok i get it after stack.pop no dfs is getting called so in this case not a perftct binary tree is drawn after dfs(2,2)
is reached we got to 2nd cond stack.pop then again 2nd condition stack.pop becuase from there only dfs was called then 
again 1st condition stack.pop then if c<o option is explored and we now check () this one, and stack is kept universal as after all options are explored it 
is popping so new options can be explored we dont need to pass stack in every call here

call stack for n = 2:
d(0,0) -> d(1,0) ->d(2,0) ->d(2,1) ->d(2,2) (base case hit return) ->d(1,1) ->d(2,1) ->d(2,2) 
                                                    |
                                            when the base case it hit program goes to 2nd conditions stack.pop so 2,1 then again it hits stack.pop 2,0
                                            then it return as program over then it reaches 1st cond dfs call completed then pop (1,0)    
                                            then if c<o is explored



Maintain a current stack/string.
Recursively add ( and ).
Don't allow invalid parenthesis states.
Stop when all parentheses are used.
Copy the current state into res.
Backtrack using pop().

at every level we basically prune this problem and use right conditions
we start open and close from 0 and base case is when both equals n thats when all combo are xplored according to our conditions

two choices are if o < n then we can append open paranthesis freely 

and if c < o that means o are used more and we have options to explore on c as well

we use stack.pop after every dfs call so all options are explored


```python
class Solution:
    def generateParenthesis(self, n: int) -> List[str]:
        

        #stack = [] ; o = 0 c = 0 is is basically same, we dont decrease instead we increase
        stack = []
        res = []

        def dfs(o,c):

            if o == c == n:
                res.append("".join(stack))
            
            if o < n:
                stack.append('(')
                dfs(o+1,c)
                stack.pop()
            if c < o: #means opens are used more
                stack.append(')')
                dfs(o,c+1)
                stack.pop()

        dfs(0,0)
        
        return res

            
            
